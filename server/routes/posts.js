const express = require('express');
const router = express.Router();

// Маршрут для создания нового поста
router.post('/', (req, res) => {
  // Логика для создания нового поста
});

// Маршрут для изменения существующего поста
router.put('/:id', (req, res) => {
  // Логика для изменения поста с указанным идентификатором
});

// Маршрут для удаления существующего поста
router.delete('/:id', (req, res) => {
  // Логика для удаления поста с указанным идентификатором
});

module.exports = router;